package preflight

// contextnav_adapter.go — stub after the analysis/contextnav package was
// removed from the standalone awareness module. Decision trace generation
// is now handled by the services module (services/golang/awareness/runtime).
// The standalone preflight package emits an empty DecisionTraces slice;
// callers that need per-finding decision traces should use the MCP service.
