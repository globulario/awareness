# Docs

## Browse locally with MkDocs

```
pip install mkdocs-material
mkdocs serve
```

Then open http://127.0.0.1:8000 in your browser.

## Browse without a generator

Open `docs/index.md` directly in any Markdown viewer or editor. All docs are plain Markdown with no preprocessor dependencies. Navigation follows the structure in `mkdocs.yml`.
